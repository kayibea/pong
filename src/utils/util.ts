export const rgba = (r: number, g: number, b: number, a = 1): string => {
  return `rgba(${r},${g},${b},${a})`;
};

export const KeyCodes = {
  ArrowUp: 38, // ArrowUp key
  ArrowDown: 40, // ArrowDown key
  W: 87, // 'W' key
  S: 83, // 'S' key
  Enter: 13, // 'Enter' key
  Space: 32, // Space bar
};
